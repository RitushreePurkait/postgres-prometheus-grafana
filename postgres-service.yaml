apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  labels:
    name: postgres-helm-operator
spec:
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      name: primary
  selector:
    name: postgres-helm-operator
